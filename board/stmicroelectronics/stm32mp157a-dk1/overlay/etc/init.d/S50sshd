#!/bin/sh

case "$1" in
    start)
        echo "Starting sshd..."
        if [ ! -f /etc/ssh/ssh_host_rsa_key ]; then
            ssh-keygen -A
        fi
        /usr/sbin/sshd
        ;;
    stop)
        echo "Stopping sshd..."
        killall sshd
        ;;
esac
